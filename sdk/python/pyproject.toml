[project]
name = "imauth"
version = "0.3.0"
description = "Python SDK for imauth — social auth manager"
requires-python = ">=3.10"
dependencies = [
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["imauth"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
