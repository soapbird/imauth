#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ -n "${IMAUTH_BIN:-}" ]; then
  exec "$IMAUTH_BIN" provider record "$@"
fi
exec cargo run --quiet --manifest-path "$repo_root/Cargo.toml" -p imauth-cli -- provider record "$@"
